
<section>
  <h1><%= @vet.full_name %></h1>

  <!-- <div class="list">
        <div class="cell">
          <table>
            <tr>
              <th>Name</th>
            </tr>
            <tr>
              <%# <td><%= @vet.full_name() %></td> %>
            </tr>
          </table>
        </div>
  </div> -->
  <h2>Responsible for:</h2>
  <div class="list">
    <% for animal in @animals %>
    <a class="item_select cell" href="/animals/<%= animal.id %>">
        <h2> <%= animal.name %> </h2>
        <p> <%= animal.type %> </p>
        <img src="" alt="">
    </a>
      <% end %>
  </div>
</section>
